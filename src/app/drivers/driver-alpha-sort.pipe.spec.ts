import { DriverAlphaSortPipe } from './driver-alpha-sort.pipe';

describe('DriverAlphaSortPipe', () => {
  it('create an instance', () => {
    const pipe = new DriverAlphaSortPipe();
    expect(pipe).toBeTruthy();
  });
});
